<View:MetadataFieldViewBase x:Class="MVVMTemplate.View.MetadataCollectionFieldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:View="clr-namespace:MVVMTemplate.View" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal">
        <StackPanel Orientation="Horizontal">
            <View:ExpandCollapseButton VerticalAlignment="Top" CommandTarget="{Binding ElementName=FieldValue}"/>
            <TextBlock x:Name="FieldLabel" VerticalAlignment="Top" Text="{Binding Path=FieldName}" TextAlignment="Right" Height="30" Padding="10 10 10 10"/>
        </StackPanel>
        <View:MetadataCollectionView x:Name="FieldValue" Height="Auto" Collection="{Binding Path=FieldValue}" Background="Lavender">
            <View:MetadataCollectionView.CommandBindings>
                <CommandBinding Command="View:Commands.ExpandCollapse"
                                Executed="CommandBinding_Executed"/>
            </View:MetadataCollectionView.CommandBindings>
        </View:MetadataCollectionView>
    </StackPanel>
</View:MetadataFieldViewBase>
